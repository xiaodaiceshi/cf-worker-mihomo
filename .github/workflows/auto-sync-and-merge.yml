name: Auto sync upstream and merge custom

on:
  schedule:
    - cron: "0 3 * * *" # 每天 UTC 03:00
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  sync-and-merge:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout fork
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Add upstream
        run: |
          git remote add upstream https://github.com/Kwisma/cf-worker-mihomo.git
          git fetch upstream
          git fetch origin

      - name: Sync main with upstream
        run: |
          git checkout main
          git pull origin main --rebase || git reset --hard origin/main
          git merge upstream/main --no-edit --allow-unrelated-histories || true
          git push origin main || echo "Main branch up to date or push failed"

      - name: Merge main into my-custom
        run: |
          git checkout my-custom
          git merge main --no-edit --allow-unrelated-histories || true
          git push origin my-custom || echo "my-custom branch up to date or push failed"
